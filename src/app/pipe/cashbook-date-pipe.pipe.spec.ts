import { CashbookDatePipePipe } from './cashbook-date-pipe.pipe';

describe('CashbookDatePipePipe', () => {
  it('create an instance', () => {
    const pipe = new CashbookDatePipePipe();
    expect(pipe).toBeTruthy();
  });
});
